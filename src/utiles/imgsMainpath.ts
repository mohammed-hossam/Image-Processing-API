import path from 'path';

export const imgsFolderPath = path.resolve(__dirname, `../../assets/`);
export const resizedImgsFolderPath = path.resolve(
    __dirname,
    '../../assets/resized-images/'
);
